import{integer,primaryKey,sqliteTable,text}from"drizzle-orm/sqlite-core";export const courseProgress=sqliteTable("course_progress",{userEmail:text("user_email").notNull(),courseId:text("course_id").notNull(),completedUnits:integer("completed_units").notNull().default(0),passed:integer("passed",{mode:"boolean"}).notNull().default(false),bestScore:integer("best_score").notNull().default(0),updatedAt:text("updated_at").notNull()},t=>[primaryKey({columns:[t.userEmail,t.courseId]})]);
