/* eslint-disable @next/next/no-img-element */
"use client";
import Link from"next/link";
import{useEffect,useMemo,useState}from"react";
import type{CSSProperties}from"react";
import{claudeCodeQuestions as questions,claudeCodeUnits as lessonUnits}from"../../claude-code-data";
import{firstCoursePassed,loadProgress,saveProgress}from"../../supabase";
type P={completedUnits:number;passed:boolean;bestScore:number};
function Celebration({score,onClose}:{score:number;onClose:()=>void}){return <div className="celebration" role="dialog" aria-modal="true" aria-label="完成所有課程"><div className="confetti" aria-hidden="true">{Array.from({length:24},(_,i)=><i key={i} style={{"--i":i} as CSSProperties}/>)}</div><section className="celebration-card stage-two"><button className="celebration-close" onClick={onClose} aria-label="關閉完成動畫">×</button><div className="celebration-glow" aria-hidden="true"><span>⌘</span></div><p>ALL COURSES COMPLETE</p><h2>恭喜你!!<br/>完成第二階段!!</h2><div className="celebration-score"><strong>{score}/8</strong><span>目前所有課程皆已通過</span></div><p className="celebration-copy">你已經不只是會問 AI，而是開始學會管理一位 AI 行動代理人。</p><a className="instagram-follow" href="https://www.instagram.com/ailifeu/" target="_blank" rel="noopener noreferrer" aria-label="前往 Instagram 追蹤 ailifeu"><span>更多 AI 變現方法</span><strong>想要找到更多AI變現的方法<br/>歡迎追蹤 <b>@ailifeu</b></strong><i>追蹤 IG →</i></a><div className="celebration-actions"><button className="primary" onClick={onClose}>查看完成成績</button><Link href="/">回到課程總目錄 →</Link></div></section></div>}
export default function CourseClient({displayName}:{displayName:string}){const[p,setP]=useState<P>({completedUnits:0,passed:false,bestScore:0}),[current,setCurrent]=useState(0),[loading,setLoading]=useState(true),[locked,setLocked]=useState(false),[quiz,setQuiz]=useState(false),[answers,setAnswers]=useState<Record<number,number>>({}),[result,setResult]=useState<number|null>(null),[celebrate,setCelebrate]=useState(false);const unit=lessonUnits[current],score=useMemo(()=>questions.reduce((s,q,i)=>s+(answers[i]===q.ok?1:0),0),[answers]);useEffect(()=>{Promise.all([firstCoursePassed(),loadProgress("claude-code")]).then(([unlocked,x])=>{setLocked(!unlocked);setP(x);setCurrent(Math.min(x.completedUnits,6))}).catch(()=>setLocked(true)).finally(()=>setLoading(false))},[]);async function save(n:P){const saved=await saveProgress("claude-code",n);setP(saved)}async function next(){if(current<p.completedUnits){setCurrent(Math.min(6,current+1));scrollTo(0,0);return}const c=Math.min(7,p.completedUnits+1);await save({...p,completedUnits:c});if(c<7)setCurrent(c);scrollTo(0,0)}async function submit(){setResult(score);if(score>=6){await save({...p,passed:true,bestScore:Math.max(score,p.bestScore)});setCelebrate(true)}}if(loading)return <main className="loading">✦<p>載入學習進度…</p></main>;if(locked)return <main className="locked-course"><section><span>🔒</span><p>COURSE 02</p><h1>Claude Code 尚未解鎖</h1><p>請先完成第一堂課的六個單元並通過測驗，才能開始第二階段。</p><Link className="primary" href="/courses/claude-01">回到第一堂課 →</Link></section></main>;return <main><header className="course-header"><Link className="brand" href="/"><b>C</b><span>Claude 新同事訓練所</span></Link><Link href="/">← 回總目錄</Link><span>嗨，{displayName}</span></header><section className="player-top code-course"><div><p>COURSE 02 · CLAUDE CODE</p><h1>把 AI 從聊天助手變成行動代理人</h1></div><div className="progress"><span>{p.completedUnits}/7 單元完成</span><i><b style={{width:`${p.completedUnits/7*100}%`}}/></i></div></section><div className="player"><aside><p>課程單元</p>{lessonUnits.map((l,i)=>{const unlock=i<=p.completedUnits,done=i<p.completedUnits;return <button key={l.n} disabled={!unlock} className={current===i?"active":done?"done":""} onClick={()=>unlock&&setCurrent(i)}><span>{done?"✓":unlock?l.n:"🔒"}</span><div><small>{l.tag}</small>{l.title}</div></button>})}<div className={p.completedUnits===7?"quiz-link ready":"quiz-link"}>{p.completedUnits===7?"🏆":"🔒"} 課後測驗</div></aside><section className="stage"><div className="stage-meta">單元 {unit.n} / 07　<span>{unit.tag}</span></div><h2>{unit.title}</h2><p className="intro">{unit.intro}</p><figure><img src={unit.image} alt={unit.alt} width="1672" height="941" loading={current===0?"eager":"lazy"}/><figcaption>CLAUDE CODE · LESSON {unit.n} · {unit.tag}</figcaption></figure><div className="body-copy">{unit.body.map(x=><p key={x}>{x}</p>)}</div><div className="points">{unit.items.map(([t,d],i)=><article key={t}><b>0{i+1}</b><div><h3>{t}</h3><p>{d}</p></div></article>)}</div><div className="practice">✎ <p><b>動手試試看</b><br/>{unit.practice}</p></div><div className="tip">💡 <p><b>教練提醒</b><br/>{unit.tip}</p></div><div className="next"><div><small>{current<6?"完成後解鎖":"完成後解鎖測驗"}</small><b>{current<6?lessonUnits[current+1]?.title:"第二堂課課後測驗"}</b></div><button onClick={next}>{current<p.completedUnits?"前往下一節 →":current<6?"完成本節，解鎖下一節 →":"完成最後一節 →"}</button></div>{p.completedUnits===7&&<section className="exam"><span>🏆</span><h2>課後測驗已解鎖</h2><p>共 8 題，答對 6 題即通過第二堂課。</p><button className="primary" onClick={()=>setQuiz(true)}>{p.passed?`重新挑戰 · 最高 ${p.bestScore}/8`:"開始測驗 →"}</button></section>}{quiz&&p.completedUnits===7&&<section className="quiz"><h2>選出最好的答案</h2><p>已作答 {Object.keys(answers).length} / 8</p>{questions.map((q,i)=><fieldset key={q.q}><legend><span>{i+1}</span>{q.q}</legend>{q.a.map((a,j)=><label key={a}><input type="radio" name={`q${i}`} onChange={()=>setAnswers(v=>({...v,[i]:j}))}/>{a}</label>)}</fieldset>)}<button className="primary" disabled={Object.keys(answers).length<8} onClick={submit}>交卷看成績</button>{result!==null&&<div className={score>=6?"result pass":"result fail"}><h2>{score}/8</h2><b>{score>=6?"恭喜你!! 完成第二階段!!":"再複習一次吧"}</b>{score>=6&&<button className="replay-celebration" onClick={()=>setCelebrate(true)}>看看你的成果</button>}</div>}</section>}</section></div>{celebrate&&<Celebration score={score} onClose={()=>setCelebrate(false)}/>}</main>}
